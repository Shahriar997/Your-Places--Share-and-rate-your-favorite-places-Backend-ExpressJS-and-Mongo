const API_KEY = "I do not have credit card to get the api";

function getCoordsForAddress(address) {
  return { 
    lat: 40.7484474, 
    lng: -73.9871516,
    msg: "I am poor. I do not have a credit card to get google API"
  };
}

module.exports = getCoordsForAddress;
